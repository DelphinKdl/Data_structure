#include <iostream>
using namespace std;
struct BT {
  int data;
  BT * right;
  BT * left;
};

BT * insert(BT * root, int data) {
  BT * node = new BT();
  if (root == NULL) {
    root = node;
    return root;
  }
  Node * temp = root;

  return root;
}
int main() {

}
